{
  "name": "deception-scenario-13",
  "description": "CloudWatch Logs Leaked Credentials — AssumeRole + GetLogEvents + FilterLogEvents",
  "scenario_number": 13,
  "scenario_name": "CloudWatch Logs Leaked Credentials Lure",
  "event_pattern": {
    "source": ["aws.sts", "aws.logs"],
    "detail-type": ["AWS API Call via CloudTrail"],
    "detail": {
      "eventName": ["AssumeRole", "GetLogEvents", "FilterLogEvents"],
      "$or": [
        {
          "requestParameters": {
            "roleName": ["log-analysis-readonly-role"]
          }
        },
        {
          "requestParameters": {
            "logGroupName": ["/prod/payment-service/application"]
          }
        }
      ]
    }
  }
}
