{
  "name": "deception-scenario-14",
  "description": "KMS Customer Data Encryption Key — AssumeRole + DescribeKey + Decrypt",
  "scenario_number": 14,
  "scenario_name": "KMS Customer Data Encryption Key Lure",
  "event_pattern": {
    "source": ["aws.sts", "aws.kms"],
    "detail-type": ["AWS API Call via CloudTrail"],
    "detail": {
      "eventName": ["AssumeRole", "DescribeKey", "Decrypt"],
      "$or": [
        {
          "requestParameters": {
            "roleName": ["kms-audit-readonly-role"]
          }
        },
        {
          "requestParameters": {
            "keyId": [{"prefix": "alias/prod-customer-data-encryption"}]
          }
        }
      ]
    }
  }
}
