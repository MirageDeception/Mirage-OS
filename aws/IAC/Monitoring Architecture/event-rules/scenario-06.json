{
  "name": "deception-scenario-06",
  "description": "Lambda Data Sync — AssumeRole + GetFunctionConfiguration + GetObject + GetSecretValue + GetParameter",
  "scenario_number": 6,
  "scenario_name": "Lambda Data Sync with Env Var Secrets Lure",
  "event_pattern": {
    "source": ["aws.sts", "aws.lambda", "aws.s3", "aws.secretsmanager", "aws.ssm"],
    "detail-type": ["AWS API Call via CloudTrail"],
    "detail": {
      "eventName": ["AssumeRole", "GetFunctionConfiguration20150331v2", "GetObject", "GetSecretValue", "GetParameter"],
      "$or": [
        {
          "requestParameters": {
            "roleName": ["lambda-ops-readonly-role"]
          }
        },
        {
          "requestParameters": {
            "functionName": ["prod-data-sync-processor"]
          }
        },
        {
          "requestParameters": {
            "bucketName": [{"prefix": "prod-data-sync-artifacts-"}]
          }
        },
        {
          "requestParameters": {
            "secretId": [{"prefix": "prod/data-sync/"}]
          }
        },
        {
          "requestParameters": {
            "name": [{"prefix": "/prod/data-sync/"}]
          }
        }
      ]
    }
  }
}
