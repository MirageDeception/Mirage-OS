{
  "name": "deception-scenario-09",
  "description": "DynamoDB Customer Profiles — AssumeRole + DescribeTable + Scan + Query + GetItem",
  "scenario_number": 9,
  "scenario_name": "DynamoDB Customer Profiles Lure",
  "event_pattern": {
    "source": ["aws.sts", "aws.dynamodb"],
    "detail-type": ["AWS API Call via CloudTrail"],
    "detail": {
      "eventName": ["AssumeRole", "DescribeTable", "Scan", "Query", "GetItem"],
      "$or": [
        {
          "requestParameters": {
            "roleName": ["customer-data-readonly-role"]
          }
        },
        {
          "requestParameters": {
            "tableName": ["prod-customer-profiles"]
          }
        }
      ]
    }
  }
}
