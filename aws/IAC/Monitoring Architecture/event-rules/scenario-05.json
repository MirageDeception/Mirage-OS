{
  "name": "deception-scenario-05",
  "description": "ECR Container Image with PII — BatchGetImage + GetDownloadUrlForLayer on decoy repo",
  "scenario_number": 5,
  "scenario_name": "ECR Container Image with PII Lure",
  "event_pattern": {
    "source": ["aws.ecr"],
    "detail-type": ["AWS API Call via CloudTrail"],
    "detail": {
      "eventName": ["BatchGetImage", "GetDownloadUrlForLayer"],
      "requestParameters": {
        "repositoryName": ["prod-payment-service"]
      }
    }
  }
}
