# Scenario 20

**Description**: Deception Scenario 20 - SSM Parameter Cross-Reference Chain. Five SSM parameters under /prod/db/* that cross-reference each other via a see_also field, creating a breadcrumb chain the attacker follows. Discovery role: infra-params-readonly-role.
