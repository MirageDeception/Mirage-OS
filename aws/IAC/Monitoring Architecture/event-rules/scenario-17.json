{
  "name": "deception-scenario-17",
  "description": "Lambda + DynamoDB Enriched User PII — AssumeRole + GetFunctionConfiguration + DescribeTable + Scan",
  "scenario_number": 17,
  "scenario_name": "Lambda + DynamoDB Enriched User PII Lure",
  "event_pattern": {
    "source": ["aws.sts", "aws.lambda", "aws.dynamodb"],
    "detail-type": ["AWS API Call via CloudTrail"],
    "detail": {
      "eventName": ["AssumeRole", "GetFunctionConfiguration20150331v2", "DescribeTable", "Scan"],
      "$or": [
        {
          "requestParameters": {
            "roleName": ["etl-ops-readonly-role"]
          }
        },
        {
          "requestParameters": {
            "functionName": ["prod-user-data-enrichment"]
          }
        },
        {
          "requestParameters": {
            "tableName": ["prod-enriched-user-profiles"]
          }
        }
      ]
    }
  }
}
